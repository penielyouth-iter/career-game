"use client";
import { useEffect, useRef, useState } from "react";
import type { BoardCell, Card, CharacterConfig, Effects, GameConfig, GameState, PlayerState, StatKey } from "@/lib/types";
import { SITE_VERSION } from "@/lib/version";

const labels:Record<StatKey,string>={experience:"經驗值",money:"金錢"};
const statKeys:StatKey[]=["experience","money"];
const stageLabels=["探索起步","專業養成","代表作實踐","使命整合"];
const perimeter=[0,1,2,3,4,5,19,6,18,7,17,8,16,9,15,14,13,12,11,10];
const diceFaces=["⚀","⚁","⚂","⚃","⚄","⚅"];
const wait=(ms:number)=>new Promise(resolve=>setTimeout(resolve,ms));
let sharedAudioContext:AudioContext|null=null;

type Pending={kind:"card";card:Card}|{kind:"cardChoice";cards:Card[];type:"chance"|"destiny"}|{kind:"overtime"}|{kind:"class"}|{kind:"info";title:string;description:string};
type StatNotice={playerId:string;stats:Record<StatKey,number>;changes:Partial<Record<StatKey,number>>;contextLabel?:string};
type MilestoneNotice={playerId:string;stage:number};
type StartPassNotice={playerId:string;reward:number;money:number};
type ResultNotice={title:string;description:string};
type ActivePlayer={id:string;player:PlayerState;character:CharacterConfig};

function audioContext(){if(typeof window==="undefined")return null;const Ctor=window.AudioContext||(window as typeof window&{webkitAudioContext?:typeof AudioContext}).webkitAudioContext;if(!Ctor)return null;sharedAudioContext??=new Ctor();void sharedAudioContext.resume();return sharedAudioContext}
function soundTone(ctx:AudioContext,frequency:number,start:number,duration:number,type:OscillatorType="sine",volume=.08){const oscillator=ctx.createOscillator();const gain=ctx.createGain();oscillator.type=type;oscillator.frequency.setValueAtTime(frequency,start);gain.gain.setValueAtTime(.0001,start);gain.gain.exponentialRampToValueAtTime(volume,start+.015);gain.gain.exponentialRampToValueAtTime(.0001,start+duration);oscillator.connect(gain).connect(ctx.destination);oscillator.start(start);oscillator.stop(start+duration+.02)}
function playClip(src:string,maxMs:number,volume=.75){if(typeof Audio==="undefined")return;const audio=new Audio(src);audio.volume=volume;void audio.play().catch(()=>{});window.setTimeout(()=>{audio.pause();audio.currentTime=0},maxMs)}
function playOutcome(kind:"cheer"|"sad"){const ctx=audioContext();if(!ctx)return;const now=ctx.currentTime;if(kind==="cheer"){[523,659,784,1047].forEach((frequency,i)=>soundTone(ctx,frequency,now+i*.2,.34,"sine",.075))}else{[392,330,262].forEach((frequency,i)=>soundTone(ctx,frequency,now+i*.28,.38,"triangle",.07))}}
function Portrait({id,size="normal",name}:{id:string;size?:"mini"|"normal"|"large";name:string}){return <span className={`character-portrait portrait-${id} portrait-${size}`} role="img" aria-label={name}/>;}
function applyEffects(p:PlayerState,effects:Effects={}){for(const [key,value] of Object.entries(effects) as [StatKey,number][])p.stats[key]+=value;p.stats.experience=Math.max(0,p.stats.experience);p.stats.money=Math.max(0,p.stats.money)}
function effectText(effects?:Effects){if(!effects||!Object.values(effects).some(Boolean))return"無數值變化";return Object.entries(effects).filter(([,v])=>v!==0).map(([k,v])=>`${labels[k as StatKey]} ${Number(v)>0?"+":""}${v}`).join("・")}
function characterOf(config:GameConfig,player?:PlayerState|false):CharacterConfig|undefined{return player?config.characters.find(c=>c.id===player.characterId):undefined}

export default function GameApp(){
 const[config,setConfig]=useState<GameConfig|null>(null);const[state,setState]=useState<GameState|null>(null);const[loading,setLoading]=useState(true);
 const[pending,setPending]=useState<Pending|null>(null);const[statNotice,setStatNotice]=useState<StatNotice|null>(null);const[milestoneNotice,setMilestoneNotice]=useState<MilestoneNotice|null>(null);const[startPassNotice,setStartPassNotice]=useState<StartPassNotice|null>(null);const[resultNotice,setResultNotice]=useState<ResultNotice|null>(null);const[dice,setDice]=useState<number|null>(null);const[rolling,setRolling]=useState(false);
 const[rollMessage,setRollMessage]=useState("");const[message,setMessage]=useState("");const[saving,setSaving]=useState(false);const startNoticeResolver=useRef<(()=>void)|null>(null);const milestoneResolver=useRef<(()=>void)|null>(null);
 useEffect(()=>{if(!state)return;const timer=setInterval(()=>{if(saving||rolling)return;fetch("/api/game",{cache:"no-store"}).then(r=>r.ok?r.json():null).then(data=>{if(!data?.state)return;setState(current=>!current||data.state.lastUpdated>current.lastUpdated?data.state:current)})},2200);return()=>clearInterval(timer)},[saving,rolling,!!state]);
 useEffect(()=>{const timer=setInterval(()=>fetch("/api/config",{cache:"no-store"}).then(r=>r.ok?r.json():null).then(data=>data?.config&&setConfig(data.config)),3000);return()=>clearInterval(timer)},[]);
 useEffect(()=>{fetch("/api/config").then(r=>{if(!r.ok)throw 0;return r.json()}).then(c=>{setConfig(c.config);return fetch("/api/game")}).then(r=>{if(!r.ok)throw 0;return r.json()}).then(g=>{setState(g.state);localStorage.setItem("career-game-cache",JSON.stringify(g.state))}).catch(()=>{const cached=localStorage.getItem("career-game-cache");if(cached)setState(JSON.parse(cached));setMessage("目前離線，先顯示本機暫存進度。")}).finally(()=>setLoading(false))},[]);
 const activeId=state?.turnOrder[state.turnIndex];const active=activeId?state?.players[activeId]:undefined;const activeCharacter=config&&characterOf(config,active);
 const save=async(next:GameState,action:string)=>{setState(next);localStorage.setItem("career-game-cache",JSON.stringify(next));setSaving(true);try{const r=await fetch("/api/game",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({state:next,action})});if(!r.ok)throw 0;const data=await r.json();setState(data.state);localStorage.setItem("career-game-cache",JSON.stringify(data.state));setMessage("")}catch{setMessage("網路暫時中斷，進度已保存在這台裝置。恢復連線後請再進行一步操作。")}finally{setSaving(false)}};
 const nextTurn=(s:GameState)=>{let attempts=0;do{s.turnIndex=(s.turnIndex+1)%s.turnOrder.length;if(s.turnIndex===0)s.round++;attempts++}while(s.players[s.turnOrder[s.turnIndex]].finishedAt!==null&&attempts<s.turnOrder.length)};
 const log=(s:GameState,text:string)=>{s.log=[text,...s.log].slice(0,40)};
 const statChanges=(before:Record<StatKey,number>,after:Record<StatKey,number>)=>Object.fromEntries(statKeys.map(key=>[key,after[key]-before[key]]).filter(([,value])=>value!==0)) as Partial<Record<StatKey,number>>;
 const notifyStats=(playerId:string,before:Record<StatKey,number>,after:Record<StatKey,number>,contextLabel?:string)=>{const changes=statChanges(before,after);if(Object.keys(changes).length)setStatNotice({playerId,stats:{...after},changes,contextLabel})};
 const confirmStartPass=()=>{setStartPassNotice(null);startNoticeResolver.current?.();startNoticeResolver.current=null};
 const confirmMilestone=()=>{setMilestoneNotice(null);milestoneResolver.current?.();milestoneResolver.current=null};
 const advanceByExperience=async(s:GameState,p:PlayerState)=>{
  if(!config||!activeId||p.finishedAt!==null)return;const character=characterOf(config,p);if(!character)return;
  const targetStage=Math.min(4,Math.floor(p.stats.experience/100));
  while(p.stage<targetStage){
   p.stage++;
   if(p.stage>=4){p.finishedAt=Date.now();log(s,`🏆 ${character.name}經驗值達到 400，完成${character.job}旅程！`)}
   else log(s,`${character.name}經驗值達到 ${p.stage*100}，進入「${character.titles[p.stage]}」`);
   setState(structuredClone(s));playOutcome("cheer");
   await new Promise<void>(resolve=>{milestoneResolver.current=resolve;setMilestoneNotice({playerId:activeId,stage:p.stage})});
  }
 };
 const settle=async(s:GameState,cell:BoardCell,p:PlayerState,before:Record<StatKey,number>)=>{applyEffects(p,cell.effects);if(cell.skip)p.skipTurns+=cell.skip;log(s,`${activeCharacter?.name}來到「${cell.label}」：${cell.description}`);await advanceByExperience(s,p);notifyStats(activeId!,before,p.stats,cell.label)};
 const drawCard=(s:GameState,p:PlayerState,type:"chance"|"destiny")=>{if(!config)return;const character=characterOf(config,p);const contentStage=Math.min(p.stage+1,4);const pool=(character?.cards||[]).filter(c=>c.contentStage===contentStage&&c.type===type);const key=`${type}-${contentStage}`;let used=p.decks[key]||[];let available=pool.filter(c=>!used.includes(c.id));if(!available.length){used=[];available=pool}const cards=[...available].sort(()=>Math.random()-.5).slice(0,Math.min(3,available.length));if(!cards.length)return;setPending({kind:"cardChoice",cards,type});log(s,`來到${type==="chance"?"機會":"命運"}卡選擇時刻`)};
 const handleStartPass=async(s:GameState,p:PlayerState)=>{if(!config||!activeCharacter||!activeId)return;const reward=config.cells.find(cell=>cell.id===0)?.startRewards?.[p.characterId]??0;p.stats.money=Math.max(0,p.stats.money+reward);log(s,`${activeCharacter.name}通過起點，獲得金錢 +${reward}`);setState(structuredClone(s));playOutcome("cheer");await new Promise<void>(resolve=>{startNoticeResolver.current=resolve;setStartPassNotice({playerId:activeId,reward,money:p.stats.money})})};
 const moveByDice=async(raw:number)=>{
  if(!state||!config||!active||!activeCharacter||pending||rolling)return;const s=structuredClone(state) as GameState;const p=s.players[activeId!];
  if(p.skipTurns>0){p.skipTurns--;log(s,`${activeCharacter.name}暫停一回合`);nextTurn(s);await save(s,"skip");return}
  setRolling(true);setDice(raw);const steps=raw*(s.accelerated?2:1);setRollMessage(`實體骰子擲出 ${raw}！${s.accelerated?`加速前進 ${steps} 格`:`前進 ${steps} 格`}`);await wait(250);
  for(let step=0;step<steps;step++){const trackIndex=perimeter.indexOf(p.position);p.position=perimeter[(trackIndex+1)%perimeter.length];playClip("/assets/audio/walking.mp3",280,.65);setState(structuredClone(s));await wait(300);if(p.position===0)await handleStartPass(s,p)}
  log(s,`${activeCharacter.name}實體骰子擲出 ${raw}${s.accelerated?`，加速前進 ${steps} 格`:""}`);const beforeEventStats={...p.stats};const cell=config.cells[p.position];setRolling(false);
  if(p.finishedAt!==null){setPending({kind:"info",title:"完成使命！",description:`${activeCharacter.name}已累積 400 經驗值，成為${activeCharacter.job}大師！`});await save(s,"finish");return}
  if(cell.type==="chance"||cell.type==="destiny"){notifyStats(activeId!,beforeEventStats,p.stats);drawCard(s,p,cell.type);await save(s,"draw");return}
  if(cell.choice==="overtime"){notifyStats(activeId!,beforeEventStats,p.stats);setPending({kind:"overtime"});await save(s,"land");return}
  if(cell.choice==="class"){notifyStats(activeId!,beforeEventStats,p.stats);setPending({kind:"class"});await save(s,"land");return}
  await settle(s,cell,p,beforeEventStats);const changes=statChanges(beforeEventStats,p.stats);if(Object.values(changes).some(value=>(value||0)<0))playOutcome("sad");else if(Object.values(changes).some(value=>(value||0)>0))playOutcome("cheer");nextTurn(s);await save(s,"roll");
 };
 const chooseCard=(card:Card)=>{if(!state||!config||!activeId)return;const s=structuredClone(state) as GameState;const p=s.players[activeId];const key=`${card.type}-${card.contentStage}`;const character=characterOf(config,p);const pool=(character?.cards||[]).filter(item=>item.contentStage===card.contentStage&&item.type===card.type);const used=p.decks[key]||[];const base=pool.every(item=>used.includes(item.id))?[]:used;p.decks[key]=[...base,card.id];setPending({kind:"card",card});log(s,`選中了${card.type==="chance"?"機會":"命運"}卡「${card.title}」`);save(s,"choose-card")};
 const resolve=async(effects?:Effects,note="事件結算",contextLabel?:string,resultDescription?:string)=>{if(!state||!activeId)return;const s=structuredClone(state) as GameState;const p=s.players[activeId];const before={...p.stats};if(effects)applyEffects(p,effects);log(s,note);await advanceByExperience(s,p);notifyStats(activeId,before,p.stats,contextLabel);if(resultDescription)setResultNotice({title:"選擇結果",description:resultDescription});setPending(null);nextTurn(s);save(s,"resolve")};
 const selectCharacter=(playerId:string,characterId:string)=>{if(!state||!config||state.started)return;const character=config.characters.find(c=>c.id===characterId);if(!character)return;const s=structuredClone(state) as GameState;s.players[playerId]={...s.players[playerId],characterId,position:0,stage:0,stats:{...character.initial},skipTurns:0,finishedAt:null,decks:{}};setState(s)};
 const start=()=>{if(!state)return;const selected=state.turnOrder.map(id=>state.players[id]?.characterId).filter(Boolean);if(selected.length!==4||new Set(selected).size!==4){setMessage("請為四位玩家選擇不重複的職業。");return}const s=structuredClone(state);s.started=true;log(s,"守護天使畢業考正式開始！");save(s,"start")};
 const moveOrder=(id:string,dir:number)=>{if(!state)return;const s=structuredClone(state);const i=s.turnOrder.indexOf(id);const j=i+dir;if(j<0||j>=s.turnOrder.length)return;[s.turnOrder[i],s.turnOrder[j]]=[s.turnOrder[j],s.turnOrder[i]];setState(s)};
 const toggleSpeed=()=>{if(!state||rolling)return;const s=structuredClone(state);s.accelerated=!s.accelerated;log(s,`加速模式已${s.accelerated?"開啟":"關閉"}`);save(s,"speed")};
 if(loading||!config||!state)return <main className="loading"><div className="spinner">🎲</div><p>正在載入生涯旅程…</p></main>;
 const activePlayers=state.turnOrder.map(id=>({id,player:state.players[id],character:characterOf(config,state.players[id])})).filter((item):item is ActivePlayer=>!!item.player&&!!item.character);
 const selectedIds=activePlayers.map(item=>item.player.characterId);
 const finished=activePlayers.filter(item=>item.player.finishedAt!==null).length;
 return <main>
  <header className="game-intro"><div className="site-tools"><span className={message?"status offline":"status"}>{message||(saving?"儲存中…":"雲端已同步")}</span><span className="version-badge">{SITE_VERSION}</span><a href="/admin" className="ghost">管理設定 ⚙️</a></div><div className="intro-copy"><p className="eyebrow">PENIEL YOUTH CAREER CAMP</p><span className="pill">守護天使畢業考</span><h1>{config.title}</h1><p>{config.story}</p></div><div className="hero-badge"><b>{finished}/4</b><span>已成為大師</span></div></header>
  {!state.started&&<section className="setup card"><div><p className="eyebrow">STEP 01</p><h2>選擇職業與出發順序</h2><p>四組守護天使各選一個不重複的職業，再用箭頭調整行動順序。</p></div><div className="order-list">{state.turnOrder.map((id,i)=>{const p=state.players[id]!;const c=characterOf(config,p);return <div className="order-item setup-player" key={id}><b>{i+1}</b><Portrait id={p.portraitId||c?.id||"goto"} name={`玩家 ${i+1}`}/><span><strong>{c?.name||"尚未選擇"}</strong><small>{c?.job||"請選擇職業"}</small></span><select value={p.characterId} onChange={e=>selectCharacter(id,e.target.value)}>{config.characters.map(character=><option value={character.id} key={character.id} disabled={selectedIds.includes(character.id)&&character.id!==p.characterId}>{character.name}・{character.job}</option>)}</select><button onClick={()=>moveOrder(id,-1)} disabled={i===0}>↑</button><button onClick={()=>moveOrder(id,1)} disabled={i===state.turnOrder.length-1}>↓</button></div>})}</div><button className="primary big" onClick={start}>開始冒險 →</button></section>}
  {state.started&&<><section className="characters-section"><div className="section-heading"><div><p className="eyebrow">GUARDIAN TEAMS</p><h2>守護對象</h2></div><span>第 {state.round} 輪</span></div><div className="characters-row">{activePlayers.map(({id,player:p,character:c})=>{const isActive=id===activeId;const nextTarget=Math.min(400,(p.stage+1)*100);return <article className={`player-card ${isActive?"active":""} ${p.finishedAt?"finished":""}`} key={id} style={{"--accent":c.color} as React.CSSProperties}><div className="player-head"><Portrait id={p.portraitId||c.id} name={c.name}/><div><small>{c.instrument}</small><h3>{c.name}</h3><b>目標・{c.job}</b></div>{isActive&&<span className="turn-tag">行動中</span>}</div><div className="stage career-stage" tabIndex={0}><span>階段 {Math.min(p.stage+1,4)}/4</span><strong>{c.titles[Math.min(p.stage,3)]||"尚未設定"}</strong><div className="career-tooltip" role="tooltip">{p.finishedAt?<><b>生涯旅程已完成</b><span>經驗值已達 400。</span></>:<><b>下個目標・{stageLabels[Math.min(p.stage+1,3)]}</b><span>經驗值達 {nextTarget} 即可進階</span></>}</div></div><div className="stats">{statKeys.map(k=><div key={k}><span>{labels[k]}</span><b>{p.stats[k]}</b></div>)}</div><div className="money">下一階段：{p.finishedAt?"完成":nextTarget}</div></article>})}</div></section><details className="journey-details card"><summary><span>📖 旅程紀錄</span><small>點擊展開・最新紀錄在上</small></summary><div className="log-list">{state.log.map((x,i)=><p key={i}><span>{i===0?"●":"○"}</span>{x}</p>)}</div></details><section className="board-stage"><div className="board">{perimeter.map((cellId,index)=>{const cell=config.cells[cellId];const pieces=activePlayers.filter(item=>item.player.position===cellId);return <div key={cellId} className={`board-cell cell-${index} ${cell.type}`}><span className="cell-icon">{cell.icon}</span><b>{cell.label}</b><div className="pieces">{pieces.map(({id,player,character})=><span key={id} className={id===activeId&&rolling?"piece-moving":""} style={{borderColor:character.color}} title={character.name}><Portrait id={player.portraitId||character.id} name={character.name} size="mini"/></span>)}</div></div>})}<div className="board-center"><p className="eyebrow">NOW PLAYING</p><Portrait id={active?.portraitId||activeCharacter?.id||"goto"} name={activeCharacter?.name||""} size="large"/><h2>{activeCharacter?.name}</h2><p>前進吧，守護天使！</p>{rollMessage&&<div className="roll-result"><b>{dice?diceFaces[dice-1]:"🎲"}</b><span>{rollMessage}</span></div>}<div className="physical-dice-picker"><b>輸入實體骰子結果</b><small>{rolling?"角色移動中…":"學生擲完骰子後，請主持人點選點數"}</small><div>{diceFaces.map((face,index)=><button key={face} className={dice===index+1?"selected":""} onClick={()=>moveByDice(index+1)} disabled={saving||rolling||!!pending} aria-label={`骰子點數 ${index+1}`}><span>{face}</span><em>{index+1}</em></button>)}</div></div><button className={`speed ${state.accelerated?"on":""}`} onClick={toggleSpeed} disabled={rolling}>⚡ 加速模式 {state.accelerated?"ON":"OFF"}</button>{startPassNotice&&!milestoneNotice&&(()=>{const p=state.players[startPassNotice.playerId];const c=characterOf(config,p);return c&&p?<div className="board-start-overlay"><section className="start-pass-dialog card" style={{"--accent":c.color} as React.CSSProperties}><div className="start-pass-icon">🎓</div><p className="eyebrow">PASSING START</p><h2>通過起點！</h2><h3>{c.name} 獲得 <strong>＋{startPassNotice.reward}</strong> 元</h3><p>目前金錢：{startPassNotice.money}</p><button className="primary big" onClick={confirmStartPass}>確認並繼續</button></section></div>:null})()}{statNotice&&!milestoneNotice&&!resultNotice&&!startPassNotice&&(()=>{const p=state.players[statNotice.playerId];const c=characterOf(config,p);return c&&p?<div className="board-stat-overlay"><section className="board-stat-dialog card" style={{"--accent":c.color} as React.CSSProperties}><div className="stat-notice-head"><Portrait id={p.portraitId||c.id} name={c.name}/><div><small>{statNotice.contextLabel?`${statNotice.contextLabel}後數值現況`:"事件結算後數值現況"}</small><h3>{c.name}・{c.job}</h3></div></div><div className="stat-notice-grid">{statKeys.map(key=>{const delta=statNotice.changes[key]||0;return <div className={delta?"changed":""} key={key}><span>{labels[key]}</span><b>{statNotice.stats[key]}</b>{delta>0&&<em className="stat-delta up">↑ +{delta}</em>}{delta<0&&<em className="stat-delta down">↓ {Math.abs(delta)}</em>}</div>})}</div><button className="primary stat-confirm" onClick={()=>setStatNotice(null)}>確認</button></section></div>:null})()}</div></div></section></>}
  {milestoneNotice&&(()=>{const p=state.players[milestoneNotice.playerId];const c=characterOf(config,p);return c&&p?<div className="milestone-backdrop"><section className="milestone-dialog card" style={{"--accent":c.color} as React.CSSProperties}><div className="milestone-sparkles">✦　⭐　✦</div><Portrait id={p.portraitId||c.id} name={c.name} size="large"/><p className="eyebrow">EXPERIENCE MILESTONE</p><h2>{milestoneNotice.stage>=4?"成為職涯大師！":"經驗值突破！"}</h2><h3>{c.name}{milestoneNotice.stage>=4?"完成職涯旅程":`進入「${c.titles[milestoneNotice.stage]}」`}</h3><p>{c.milestones[milestoneNotice.stage-1]||"經驗累積帶來新的生涯階段。"}</p><button className="primary big" onClick={confirmMilestone}>繼續旅程</button></section></div>:null})()}
  {resultNotice&&!milestoneNotice&&<div className="result-notice-backdrop"><section className="result-notice-dialog card"><div className="result-notice-icon">✨</div><p className="eyebrow">RESULT</p><h2>{resultNotice.title}</h2><p>{resultNotice.description}</p><button className="primary big" onClick={()=>setResultNotice(null)}>確認</button></section></div>}
  {pending&&<div className="modal-backdrop"><section className={`modal card ${pending.kind==="cardChoice"?"card-choice-modal":""}`}>{pending.kind==="cardChoice"&&<><span className={`card-type ${pending.type}`}>{pending.type==="chance"?"✨ 機會卡":"🌙 命運卡"}</span><h2>{pending.type==="chance"?"機會來臨！":"命運在你手中"}</h2><p>請守護天使選擇一張牌，看看旅程將發生什麼事！</p><div className="card-choice-grid">{pending.cards.map((card,index)=><button key={card.id} className={`mystery-card ${pending.type}`} aria-label={`選擇第 ${index+1} 張${pending.type==="chance"?"機會":"命運"}卡`} onClick={()=>chooseCard(card)}><span className="card-back-symbol">{pending.type==="chance"?"✦":"☾"}</span><b>{pending.type==="chance"?"機會":"命運"}</b><small>選擇這張</small></button>)}</div><p className="choice-footnote">本階段抽過的卡片，在全部抽完前不會重複。</p></>}{pending.kind==="card"&&(()=>{const card=pending.card;const mode=card.resolutionMode??(card.task?"task":"automatic");return <><span className={`card-type ${card.type}`}>{card.type==="chance"?"✨ 機會卡":"🌙 命運卡"}</span><h2>{card.title}</h2><p>{card.description}</p>{card.instruction&&<div className="instruction-box"><b>🎬 現場動態任務</b><span>{card.instruction}</span></div>}{mode==="choice"?<><div className="choice-hint">請守護天使討論後選擇一項</div><div className="modal-actions choice-actions"><button onClick={()=>resolve(card.optionA?.effects,`選擇「${card.optionA?.label}」：${card.title}`,undefined,card.optionA?.resultDescription||"真好！")} className="ghost"><b>{card.optionA?.label||"選項一"}</b><small>{effectText(card.optionA?.effects)}</small></button><button onClick={()=>resolve(card.optionB?.effects,`選擇「${card.optionB?.label}」：${card.title}`,undefined,card.optionB?.resultDescription||"真好！")} className="primary"><b>{card.optionB?.label||"選項二"}</b><small>{effectText(card.optionB?.effects)}</small></button></div></>:mode==="task"?<><div className="effect-comparison"><span>失敗：{effectText(card.failureEffects)}</span><span>成功：{effectText(card.successEffects)}</span></div><div className="modal-actions"><button onClick={()=>resolve(card.failureEffects,`${card.failureLabel||"任務失敗"}：${card.title}`)} className="ghost">{card.failureLabel||"任務失敗"}</button><button onClick={()=>resolve({...card.effects,...card.successEffects},`${card.successLabel||"任務成功"}：${card.title}`)} className="primary">{card.successLabel||"任務成功"}</button></div></>:<><div className="effect-box">{effectText(card.effects)}</div><button className="primary wide" onClick={()=>resolve(card.effects,`完成卡片：${card.title}`)}>套用結果</button></>}</>})()}{pending.kind==="overtime"&&<><span className="card-type destiny">🌃 人生選擇</span><h2>今晚要加班嗎？</h2><p>努力可以帶來收入，也會累積實務經驗。</p><div className="effect-box">經驗值 +20・金錢 +200</div><div className="modal-actions"><button className="ghost" onClick={()=>resolve(undefined,"婉拒加班，選擇休息")}>婉拒加班</button><button className="primary" onClick={()=>resolve({experience:20,money:200},"接受加班","加班格")}>接受加班</button></div></>}{pending.kind==="class"&&<><span className="card-type chance">🧑‍🏫 投資自己</span><h2>上課進修</h2><p>支付 100 元，獲得 30 經驗值。</p><div className="effect-box">金錢 -100・經驗值 +30</div><div className="modal-actions"><button className="ghost" onClick={()=>resolve(undefined,"這次先不上課")}>這次先不上課</button><button className="primary" onClick={()=>active&&active.stats.money<100?setMessage("金錢不足 100，無法購買課程。"):resolve({money:-100,experience:30},"上課進修","上課")}>支付上課</button></div></>}{pending.kind==="info"&&<><span className="card-type chance">✨ 特別事件</span><h2>{pending.title}</h2><p>{pending.description}</p><button className="primary wide" onClick={()=>resolve(undefined,pending.title)}>繼續旅程</button></>}</section></div>}
 </main>
}
